# Ml_Kelompok-02

pip install --upgrade pip

pip install tensorflow opencv-python numpy pandas tqdm matplotlib seaborn scikit-learn

pip install deepface keras-tuner pyngrok tensorflowjs kaggle

input token kaggle(kaggle.json)
